// libs
import React from "react";
// others
import "./style.scss";

const ContentKeyword = (props) => (
  <span className="content-keyword">{props.children}</span>
);

export default ContentKeyword;
